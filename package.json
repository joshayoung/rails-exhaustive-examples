{
  "name": "rails-exhaustive-examples",
  "private": true,
  "scripts": {
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "{app,spec}/**/*.rb": [
      "bundle exec rubocop --safe-autocorrect",
      "git add"
    ]
  },
  "dependencies": {
    "@rails/webpacker": "^4.0.7"
  },
  "devDependencies": {
    "husky": "^3.0.9",
    "lint-staged": "^10.0.0-beta.3",
    "webpack-dev-server": "^3.9.0"
  }
}
